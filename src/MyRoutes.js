import React from 'react'
import { BrowserRouter } from 'react-router-dom'

const MyRoutes = () => {
  return (
    <BrowserRouter>
        <Routes>
            <route></route>
        </Routes>
    </BrowserRouter>
  )
}

export default MyRoutes